# Python dependencies for STOMP Node Output GUI Viewer
# Install with: pip install -r requirements.txt

# Required libraries
pandas>=1.3.0
matplotlib>=3.3.0

# Optional (for drag-and-drop file support)
# If not installed, the app will still work but without drag-and-drop
tkinterdnd2>=0.3.0
